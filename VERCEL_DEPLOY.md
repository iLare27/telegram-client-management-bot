# 🚀 Деплой на Vercel

## 📋 Что изменилось для Vercel

1. ✅ **Webhook режим** вместо long polling
2. ✅ **Vercel Postgres** вместо SQLite
3. ✅ **Serverless функция** `api/webhook.ts`
4. ✅ **Отдельные хендлеры** для Vercel (`*-vercel.ts`)

---

## 🔧 Шаг 1: Подготовка проекта

Все файлы уже созданы:
- ✅ `vercel.json` - конфигурация Vercel
- ✅ `api/webhook.ts` - serverless функция
- ✅ `src/database/vercel-storage.ts` - Postgres адаптер
- ✅ `src/handlers/*-vercel.ts` - хендлеры для Vercel

---

## 🌐 Шаг 2: Деплой на Vercel

### Вариант A: Через UI (рекомендуется)

1. Откройте https://vercel.com/new
2. Импортируйте ваш GitHub репозиторий
3. Vercel автоматически обнаружит настройки из `vercel.json`
4. Нажмите **Deploy**

### Вариант B: Через CLI

```bash
# Установите Vercel CLI
npm install -g vercel

# Деплой
vercel
```

---

## 🗄️ Шаг 3: Настройка Vercel Postgres

### В Vercel Dashboard:

1. Перейдите в ваш проект
2. Вкладка **Storage** → **Create Database**
3. Выберите **Postgres**
4. Регион: выберите ближайший к вашим пользователям
5. Нажмите **Create**

Vercel автоматически добавит environment variables:
- `POSTGRES_URL`
- `POSTGRES_PRISMA_URL`
- `POSTGRES_URL_NON_POOLING`

**База данных инициализируется автоматически** при первом запросе!

---

## ⚙️ Шаг 4: Настройка Environment Variables

В **Vercel Dashboard → Settings → Environment Variables** добавьте:

| Имя переменной | Значение | Описание |
|----------------|----------|----------|
| `BOT_TOKEN` | `7550321896:ABC...` | Ваш токен от @BotFather |
| `GROUP_CHAT_ID` | `-1003264319941` | ID вашей группы |

> **Примечание:** `POSTGRES_URL` добавляется автоматически при создании БД

После добавления **переделаплойте проект** (Vercel сделает это автоматически).

---

## 🔗 Шаг 5: Настройка Webhook

После деплоя получите URL вашего приложения:
```
https://your-app-name.vercel.app
```

### Установите webhook:

**Вариант 1: Через браузер**

Откройте в браузере (замените `<YOUR_BOT_TOKEN>` и `<YOUR_VERCEL_URL>`):

```
https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook?url=<YOUR_VERCEL_URL>/api/webhook
```

Пример:
```
https://api.telegram.org/bot7550321896:ABCdef.../setWebhook?url=https://telegram-bot.vercel.app/api/webhook
```

**Вариант 2: Через curl**

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook?url=<YOUR_VERCEL_URL>/api/webhook"
```

**Ожидаемый ответ:**
```json
{
  "ok": true,
  "result": true,
  "description": "Webhook was set"
}
```

---

## ✅ Шаг 6: Проверка

### 1. Проверьте webhook:

```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"
```

Должно быть:
```json
{
  "ok": true,
  "result": {
    "url": "https://your-app.vercel.app/api/webhook",
    "has_custom_certificate": false,
    "pending_update_count": 0
  }
}
```

### 2. Проверьте бота:

1. Откройте приват с ботом
2. Отправьте `/start`
3. Должна создаться тема в группе ✅
4. Отправьте сообщение → должно попасть в тему ✅

---

## 📊 Мониторинг

### Логи в Vercel:

1. **Vercel Dashboard → Deployments**
2. Выберите последний деплой
3. Вкладка **Functions** → выберите `api/webhook`
4. **Logs** - здесь все логи бота

### Проверка базы данных:

```bash
# Установите Vercel CLI
npm install -g vercel

# Подключитесь к БД
vercel env pull .env.local
```

Затем используйте psql или любой Postgres клиент.

---

## 🐛 Troubleshooting

### Проблема: "Webhook not set"

**Решение:**
```bash
# Проверьте URL
curl "https://api.telegram.org/bot<TOKEN>/getWebhookInfo"

# Установите заново
curl -X POST "https://api.telegram.org/bot<TOKEN>/setWebhook?url=https://your-app.vercel.app/api/webhook"
```

### Проблема: "Database connection error"

**Решение:**
1. Убедитесь что Postgres база создана в Vercel
2. Проверьте что `POSTGRES_URL` есть в Environment Variables
3. Переделаплойте проект

### Проблема: "Function timeout"

**Решение:**
Vercel Free план: 10 сек timeout
Vercel Pro план: 60 сек timeout

Если нужно больше - используйте VPS.

### Проблема: Bot не отвечает

**Решение:**
1. Проверьте логи в Vercel Dashboard
2. Проверьте что BOT_TOKEN и GROUP_CHAT_ID правильные
3. Проверьте что бот админ в группе с правами "Manage Topics"

---

## 🔄 Обновление кода

```bash
# Закоммитьте изменения
git add .
git commit -m "Update bot"
git push

# Vercel автоматически переделаплоит
```

---

## 💰 Стоимость

**Vercel:**
- Free tier: 100GB bandwidth/месяц
- Postgres: $0.1/GB хранилище + $0.2/1M строк прочитано

**Для небольшого бота:** практически бесплатно! 🎉

---

## 📝 Различия с локальной версией

| Аспект | Локально (VPS) | Vercel |
|--------|-----------------|--------|
| **Режим** | Long Polling | Webhooks |
| **База** | SQLite | Postgres |
| **Файлы** | `src/handlers/start.ts` | `src/handlers/start-vercel.ts` |
| **Запуск** | `npm run dev` | Автоматически |
| **Масштаб** | 1 инстанс | Auto-scaling |

---

## 🎯 Готово!

Ваш бот теперь работает на Vercel:
- ✅ Serverless
- ✅ Auto-scaling
- ✅ Postgres база
- ✅ Бесплатно (или почти)

**URL вашего бота:** `https://your-app.vercel.app/api/webhook`

Enjoy! 🚀

